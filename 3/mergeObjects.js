// scrivere una funzione chiamata mergeObjects che 
// dati due oggetti (o mappe) in ingresso
// ritorni un unico oggetto
// eventuali duplicati devono essere sovrascritti dal contenuto
// del secondo oggetto

// non si puo' usare lo spread operator (...)

// ad esempio per {a:1} e {b:2} ritorna {a:1,b:2}
// ad esempio per {a:1} e {a:2,b:2} ritorna {a:2,b:2}
// ad esempio per {} e {a:1} ritorna {a:1}
// ad esempio per {a:1} e {} ritorna {a:1}


function mergeObjects(o1, o2) {
}

module.exports = mergeObjects